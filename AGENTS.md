Coding standards:
- Every new function must include type hints.
- Every new function must include a Google-style docstring.
- If editing an existing function, update the signature and docstring to reflect the new behavior.